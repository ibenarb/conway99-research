# Conway99 — storage incident and bounded restart, 2026-09-15

Evidence basis: user-supplied terminal output and logs in the conversation; this record is not a fresh inspection of RB-CUBE or its NAS. Dates in file names are retained as reported.

## Established state

The C2 calibration `calibration_20260914_205319_714981` launched 11 jobs with 28,800 seconds per job and LRAT output. The last supplied status still listed all jobs running, about 99 GiB free inside Linux and about 216 GiB of new proof output. The Windows C: volume nevertheless filled. Monitoring Linux free space alone did not protect the host VHDX volume. No successful production SAT/UNSAT result or proof verification was established from these logs.

Ninety non-proof files were copied to `R:\Conway99\C2_Recovery_20260915\calibration_20260914_205319_714981`; content verification passed, recorded in `verification_20260915_070718.json`. Eleven production partial proof files were removed with the user's executed cleanup procedure; receipt `partial_proof_cleanup_20260915_070948.json`. These removed files are not available for later certification. Small controls are not production exclusions.

After trim/shutdown/DiskPart compaction, normal Ubuntu attachment failed with `0x80070570`. A read-only Windows CHKDSK scan reported no NTFS problems. Read-only attachment through Windows PhysicalDrive1 and Debian exposed the ext4 filesystem; `e2fsck -f -n` completed with only an optional extent-tree optimization suggestion. This did not verify all file contents. A subsequent archive read stalled and kernel output reported repeated virtual-storage read errors. Neither physical disk failure nor the precise VHDX failure mechanism has been proved.

The incomplete `Recovery_20260915_105428/conway99_workspace.tar.partial` was removed. The user ended further recovery attempts. The old Ubuntu VHDX was retained, not declared recovered or deleted. Debian was installed and made the working distribution; account `rb_debian` is intended for the restart. No new production scout was launched as part of preparing this code.

## Backup boundaries

The supplied `archive_verify_20260914_112243/summary.json` verifies 7,354 files of `task03_fullcert_1.1_20260829`, and `relocation.json` records that directory as archived and linked under `R:\Conway99`. This receipt does NOT certify that the later K66 production LRAT corpus is fully backed up outside the old VHDX. Git preserves source, reports and recorded results according to their individual committed scope; it is not automatically a copy of every local proof file. Do not delete the retained VHDX on the basis of the FULLCERT receipt alone.

## Research implication

The interrupted scouts establish no involution exclusion. Existing K66 arithmetic/orbit and encoder reports keep their previously stated scope; the incident supplies neither a new proof nor a refutation. Production proof availability and fresh replay remain separate from archived verification messages. No global symmetry exclusion follows.

The next step is a bounded no-proof comparison of three exhaustive C2 partitions with baseline controls. New UNSAT messages will be treated as uncertified leads. Any useful cases must later be rerun with separately budgeted proof output and checked certificates. A longer experiment is a decision after calibration, not an automatic continuation.
